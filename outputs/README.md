# Outputs

This directory stores generated artifacts from local runs, such as:

- dispatch plots
- battery SOC charts
- comparison exports
- benchmark result snapshots

The default demo writes charts here so the repository structure stays organized.
